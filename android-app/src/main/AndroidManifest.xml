<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- NFC permissions -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="true" />
    <uses-feature
        android:name="android.hardware.nfc.hce"
        android:required="true" />

    <!-- Bluetooth permissions for PN532 HC-06 adapter -->
    <uses-permission android:name="android.permission.BLUETOOTH" 
        android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" 
        android:maxSdkVersion="30" />
    
    <!-- Bluetooth permissions for Android 12+ (API 31+) -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN" />
    
    <!-- Location permission for Bluetooth scanning -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <!-- USB permissions for PN532 direct USB connection -->
    <uses-permission android:name="android.permission.USB_PERMISSION" />
    <uses-feature
        android:name="android.hardware.usb.host"
        android:required="false" />

    <!-- Internet for logging and updates -->
    <uses-permission android:name="android.permission.INTERNET" />
    
    <!-- Storage for card database and backup -->
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <!-- Wake lock to prevent sleep during card operations -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    
    <!-- Notifications for health alerts (Phase 3 Days 7-8) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".application.NfSp00fApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/nfspoof3"
        android:label="nf-sp00f33r"
        android:roundIcon="@drawable/nfspoof3"
        android:supportsRtl="true"
        android:theme="@style/Theme.NfSp00f33r"
        tools:targetApi="31">

        <!-- Splash Screen Activity -->
        <activity
            android:name=".activities.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.NfSp00f33r.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Main Activity - Dual Mode NFC Support -->
        <activity
            android:name=".activities.MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/Theme.NfSp00f33r">
            
            <!-- 
                DUAL-MODE NFC ARCHITECTURE:
                1. HCE Service (background): Emulates cards via EnhancedHceService
                2. Reader Mode (foreground): Scans cards via ForegroundDispatch
                
                Intent filters provide fallback when foreground dispatch is not active
            -->
            
            <!-- EMV/ISO-DEP Card Reading Intent Filter -->
            <intent-filter android:priority="1000">
                <action android:name="android.nfc.action.TECH_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            
            <!-- NDEF Discovery Intent Filter -->
            <intent-filter android:priority="999">
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="*/*" />
            </intent-filter>
            
            <!-- Generic Tag Discovery (lowest priority fallback) -->
            <intent-filter android:priority="1">
                <action android:name="android.nfc.action.TAG_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            
            <!-- NFC Technology Filter for EMV Cards -->
            <meta-data
                android:name="android.nfc.action.TECH_DISCOVERED"
                android:resource="@xml/nfc_tech_filter" />
        </activity>

        <!-- Enhanced HCE Service for EMV Emulation -->
        <!-- 
            This service runs in BACKGROUND and emulates payment cards
            It works independently from the Reader Mode in MainActivity
            Android automatically routes external readers to this service
        -->
        <service
            android:name=".cardreading.EnhancedHceService"
            android:exported="true"
            android:enabled="true"
            android:permission="android.permission.BIND_NFC_SERVICE">
            <intent-filter android:priority="1000">
                <action android:name="android.nfc.cardemulation.action.HOST_APDU_SERVICE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
            <meta-data
                android:name="android.nfc.cardemulation.host_apdu_service"
                android:resource="@xml/apduservice" />
        </service>

        <!-- USB Device Filter for PN532 -->
        <activity
            android:name=".hardware.UsbDeviceActivity"
            android:exported="false">
            <intent-filter>
                <action android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED" />
            </intent-filter>
            <meta-data
                android:name="android.hardware.usb.action.USB_DEVICE_ATTACHED"
                android:resource="@xml/device_filter" />
        </activity>

        <!-- File Provider for secure file sharing -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Debug Command Receiver - AI Agent Interface (Debug builds only) -->
        <receiver
            android:name=".debug.DebugCommandReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="com.nfsp00f33r.app.DEBUG_COMMAND" />
            </intent-filter>
        </receiver>

    </application>

</manifest>
